
[build]
  command = "unzip halallearn_fixed.zip -d extracted && cd extracted/newproject && npm install && npm run build"
  publish = "extracted/newproject/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
